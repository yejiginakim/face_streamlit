# ---- Core ----
numpy==1.26.4
Pillow==10.4.0
pillow-avif-plugin==1.4.6
streamlit==1.38.0
huggingface_hub==0.25.2

# ---- OpenCV (플랫폼 분기) ----
opencv-python==4.10.0.84; platform_system == "Darwin"
opencv-python-headless==4.10.0.84; platform_system != "Darwin"

# ---- MediaPipe ----
mediapipe==0.10.21

# ---- TensorFlow (플랫폼 분기) ----
# Streamlit Cloud(Linux)
tensorflow-cpu==2.15.0.post1; platform_system != "Darwin"

# macOS Apple Silicon(arm64)
tensorflow-macos==2.13.0; platform_system == "Darwin" and platform_machine == "arm64"
tensorflow-metal==1.1.0; platform_system == "Darwin" and platform_machine == "arm64"

# ---- Others ----
h5py==3.10.0
scikit-learn==1.3.2
